<div class='page-header'>
  <h1>Books</h1>
</div>

<%= render partial: 'owned_books', locals: { books: @owned_books, users: @users } %>
<%= render partial: 'lent_books', locals: { books: @lent_books } %>
<%= render partial: 'borrowed_books', locals: { books: @borrowed_books } %>

<%= link_to t('.new', default: t('helpers.links.new')),
            new_book_path,
            class: 'btn btn-primary' %>
